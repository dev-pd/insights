# ============================================================
# Compose-level configuration
# Read by docker-compose.yml itself, not by the containers.
# ============================================================

# Host port that nginx listens on. Browser hits http://localhost:${FRONTEND_PORT}
FRONTEND_PORT=8080
